# Lorenzo — тестовые зависимости (для pytest в CI и локально)
# pip install -r requirements-test.txt

pytest>=8.0.0
fastapi>=0.111.0      # нужен test_gateway.py (TestClient)
httpx>=0.27.0         # FastAPI TestClient транспорт
uvicorn[standard]>=0.29.0  # импортируется в gateway.py
pydantic>=2.0.0       # FastAPI модели
beautifulsoup4>=4.12.0    # extract_mhtml.py, verify_coverage.py (module-level import)
